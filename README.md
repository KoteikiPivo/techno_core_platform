Документация API через Swagger UI: [koteikipivo.ru/techno/docs](https://koteikipivo.ru/techno/docs)<br/>
Также, для тестирования без установки последняя версия API развернута по ссылке https://koteikipivo.ru/techno

Задание: **Студент 1 Бобков Михаил — «Ядро платформы (Core Platform)»**.

# 1. Постановка задачи
Разработать backend-сервис — ядро модульной BI-платформы, которое обеспечивает хранение, валидацию и управление описаниями дашбордов в формате JSON. Сервис должен предоставлять REST API для выполнения CRUD-операций, проверять корректность входных данных по утверждённой схеме и отдавать списки доступных источников данных и виджетов.

# 2. Анализ предметной области
Перед началом работы были выбраны модули Pydantic и FastAPI для основного задания, а также решено использовать SQLite для хранения данных и SQLAlchemy для простого обращения к базе данных. Данные решения были приняты за счет интереса изучения и популярности среди похожих проектов.

# 3. Обзор аналогов
Для всех рассмотренных аналогов был использован архитектурный стиль REST API.
- В Metabase для хранения данных используется PostgreSQL и данные валидируются на уровне базы данных.
- В Apache Superset, как и в нашем проекте, используется SQLAlchemy, а данные валидируются на уровне приложения при помощи Pydantic.
- В Grafana дашборды хранятся в виде JSON-документов в базе данных, а их валидация происходит на этапе сохранения через проверку соответствия внутренней JSON-схеме.

# 4. Функциональные требования
- Создание дашборда — принять JSON-описание, проверить его на корректность, сохранить.
- Получение дашборда — отдать сохранённое описание по его ID.
- Список дашбордов — отдать список всех сохранённых дашбордов.
- Обновление и удаление дашборда.
- Валидация: если пришёл “кривой” JSON (например, без обязательного поля), сервис должен вернуть понятную ошибку, а не просто упасть.
- Список источников данных и виджетов — на первых порах можно вернуть просто список названий (в будущем сюда подключатся реальные модули других студентов).

# 5. Архитектурное решение
```mermaid
graph TD
    API[FastAPI REST API]
    Auth[Модуль авторизации<br/>check_role / require_designer]
    Validator[Валидация Pydantic<br/>DashboardCreate / DashboardResponse]
    CRUD[CRUD операции<br/>create / read / update / delete]
    ORM[SQLAlchemy ORM]
    DB[(SQLite)]

    API --> Auth
    API --> Validator
    API --> CRUD
    CRUD --> ORM
    ORM --> DB
```

Ядро разрабатывалось по трёхуровневой архитектуре, делящее его на уровень представления (REST API), уровень логики (CRUD операции и аутентификация) и уровень данных (SQLite база данных и обращение к ней через SQLAlchemy). <br/>
Модуль аутентификации является работой номер 6. В ходе разработки оказалось удобно встроить его в существующий код ядра платформы, поэтому он не является внешним модулем.

# 6. Проектирование данных
**[Данные модуля хранятся в SQLite базе данных с данной схемой](docs/architecture.md)**<br/><br/>
Формат данных в JSON:
```json
{
  "schema_version": "1.0",
  "dashboard_id": "sales-overview",
  "title": "Обзор продаж",
  "layout": {
    "type": "grid",
    "columns": 12
  },
  "widgets": [
    {
      "widget_id": "w1",
      "type": "line_chart",
      "position": { "x": 0, "y": 0, "w": 6, "h": 4 },
      "config": {
        "x_field": "date",
        "y_field": "revenue",
        "title": "Выручка по дням"
      }
    }
  ],
  "datasets": [
    {
      "dataset_id": "sales_2026",
      "columns": [
        { "name": "date", "type": "date" },
        { "name": "revenue", "type": "number" }
      ],
      "rows": [
        ["2026-01-01", 120000],
        ["2026-01-02", 98000]
      ]
    }
  ]
}
```
Формат тестового датасета (для виджетов):
```json
{
  "dataset_id": "sales_2026",
  "columns": [
    { "name": "date", "type": "date" },
    { "name": "revenue", "type": "number" }
  ],
  "rows": [
    ["2026-01-01", 120000],
    ["2026-01-02", 98000]
  ]
}
```

# 7. Описание интерфейсов

Модуль реализует следующие контракты платформы:

- **REST API** — HTTP-эндпоинты для управления дашбордами (CRUD-операции).
- **JSON-схема дашборда версии 1.0** — единый формат описания дашборда, согласованный с остальными модулями.
- **Единый формат данных Dataset** — контракт 2.3.2, используемый для передачи данных виджетам.

Модуль **потребляет** контракты других студентов:

| Контракт | От кого | Описание |
|----------|---------|----------|
| **Модуль пользователей** | Студент 6 | Роли пользователей (Designer/Viewer), авторизация через заголовки |
| **Библиотека виджетов** | Студент 4 | Структура `config` для каждого типа виджета |
| **Конструктор дашбордов** | Студент 5 | Формат JSON-описания, который генерирует конструктор |

Ядро платформы выступает центральным узлом интеграции: оно принимает данные от Data Providers, проверяет права через модуль пользователей, отдаёт данные в Widget Library и принимает JSON-описания от Dashboard Designer.


# 8. Описание API
URL для API: `http://127.0.0.1:8011/api/v1`

## Таблица эндпоинтов
| Метод | URL | Назначение | Доступно роли Viewer |
|-------|-----|------------|-----------------|
| POST | `/dashboards` | Создать дашборд | ❌ Нет |
| GET | `/dashboards` | Получить все дашборды | ✅ Да |
| GET | `/dashboards/{dashboard_id}` | Получить дашборд по ID | ✅ Да |
| PUT | `/dashboards/{dashboard_id}` | Обновить дашборд | ❌ Нет |
| DELETE | `/dashboards/{dashboard_id}` | Удалить дашборд | ❌ Нет |
| GET | `/sources` | Список источников данных | ✅ Да |
| GET | `/widgets` | Список виджетов | ✅ Да |

## Примеры запросов
### Создать дашборд
```bash
curl -X POST http://127.0.0.1:8011/api/v1/dashboards \
  -H "Content-Type: application/json" \
  -H "User-id: test-designer" \
  -H "Role: Designer" \
  -d '{
    "dashboard_id": "sales-overview",
    "title": "Обзор продаж",
    "layout": {"type": "grid", "columns": 12},
    "widgets": [
      {
        "widget_id": "w1",
        "type": "line_chart",
        "position": {"x": 0, "y": 0, "w": 6, "h": 4},
        "config": {"x_field": "date", "y_field": "revenue", "title": "Выручка по дням"}
      }
    ],
    "datasets": [
      {
        "dataset_id": "sales_2026",
        "columns": [{"name": "date", "type": "date"}, {"name": "revenue", "type": "number"}],
        "rows": [["2026-01-01", 120000], ["2026-01-02", 98000]]
      }
    ]
  }'
  ```
### Получить дашборд по ID
```bash
curl http://127.0.0.1:8011/api/v1/dashboards/sales-overview
```
### Удалить дашборд
```bash
curl -X DELETE http://127.0.0.1:8011/api/v1/dashboards/sales-overview
```

# 9. Структура проекта
```bash
app/
├── __init__.py                  # Маркер пакета
├── main.py                      # Точка входа, создание таблиц
├── database.py                  # Подключение к SQLite, модели
├── schemas.py                   # Pydantic-схемы (валидация)
├── crud.py                      # CRUD-операции с БД
├── routes.py                    # Эндпоинты API
└── auth.py                      # Ролевая модель (Designer/Viewer)

tests/
├── __init__.py
└── test_api.py                  # Автотесты с pytest

project root/
├── Makefile                     # Команды: init, run, test, clean
├── requirements.txt             # Зависимости проекта
├── README.md                    # Документация
└── dashboards.db                # Файл SQLite БД (создаётся при запуске, удаляется с make clean)
```

# 10. Инструкция по запуску
### Клонирование репозитория
```bash
git clone https://github.com/KoteikiPivo/techno_core_platform.git
cd techno_core_platform
```
### Установка зависимостей
```bash
make init
```
### Запуск
```bash
make run
```
### Тестирование
```bash
make test
```
### Очистка
```bash
make clean  # также удалится dashboards.db
```
API будет доступен по ссылке [127.0.0.1:8011](http://127.0.0.1:8011/), а документация по [127.0.0.1:8011/docs](http://127.0.0.1:8011/docs)

# 11. Результаты тестирования
### Тестовое покрытие

Написаны автоматические тесты с использованием pytest и FastAPI TestClient, покрывающие все основные сценарии работы API:

| Категория | Количество тестов | Описание |
|-----------|-------------------|----------|
| Авторизация и роли | 5 | Designer может создавать, Viewer — нет |
| Создание дашборда | 3 | Успех, дубликат (409), невалидный ID (422) |
| Получение дашборда | 2 | Успех, не найден (404) |
| Получение списка | 1 | Пагинация, корректное количество |
| Обновление дашборда | 2 | Успех, несовпадение ID (400) |
| Удаление дашборда | 2 | Успех, не найден (404) |
| Метаданные | 2 | `/sources`, `/widgets` возвращают корректные списки |
| **Всего** | **17** | **Все тесты проходят успешно** |

### Тестовые данные

Для тестов используется in-memory SQLite-база, создаваемая перед каждым запуском. Это обеспечивает изолированность и повторяемость тестов.


# 12. Выводы
За время практики выполнены все задачи индивидуального задания. Разработан backend-сервис на FastAPI с полным набором CRUD-операций, интегрированный с ролевой моделью доступа. Получен практический опыт проектирования REST API, валидации данных через Pydantic, работы с SQLAlchemy и написания автотестов.

# 13. Перспективы развития
В данный момент, модулю предлагаются следующие улучшения:

- **Версионирование дашбордов** — хранить историю изменений с возможностью отката к предыдущей версии.
- **Поиск дашбордов** — по названию, автору, тегам, полнотекстовый поиск по содержимому.
- **Пагинация и фильтрация** — добавить фильтры по автору, дате создания, типу виджетов.
